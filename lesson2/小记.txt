主要教学fork()的效果以及在父子进程里面返回值是什么 -fork1
?父子进程的内存独立性，甚至可以想一想写时复制的操作是如何进行的


讨论fork如何进行父子进程调度，2.6后默认父先 -fork2
*** 不能根据子进程打印先就说子进程先运行 ***
？老实说这里很抽象，不过vfork可以确保子先

父进程子进程进一步讨论似了父进程和子进程的关系 -fork3

没啥好说的wiat -wait